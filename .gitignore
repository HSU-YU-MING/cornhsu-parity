# .NET
bin/
obj/
*.user
.vs/

# Parity 本機產物(報告、Figma 快取)
.parity/

# demo 的 map 檔由 parity map 互動產生,保持示範的「未配對」教學情境
samples/demo/parity.map.json
# demo 的 baseline 由 parity baseline save 產生,不進版控(真實專案的 parity.baseline.db 才要 commit)
samples/demo/parity.baseline.db

# Node(M3 報告 UI 用)
node_modules/
dist/

# 環境變數 / secrets
.env
*.local.json
npm/dist/
